create table t_item_000
(

    id          bigint auto_increment COMMENT '自动增长id',

    item_name   varchar(10) not null default '' COMMENT 'item名称',

    item_count  int         not null default 0 COMMENT 'item数量',

    create_time DATETIME    not null COMMENT '创建时间',
    PRIMARY KEY (id)
);
create table t_item_001
(

    id          bigint auto_increment COMMENT '自动增长id',

    item_name   varchar(10) not null default '' COMMENT 'item名称',

    item_count  int         not null default 0 COMMENT 'item数量',

    create_time DATETIME    not null COMMENT '创建时间',
    PRIMARY KEY (id)
);
create table t_item_002
(

    id          bigint auto_increment COMMENT '自动增长id',

    item_name   varchar(10) not null default '' COMMENT 'item名称',

    item_count  int         not null default 0 COMMENT 'item数量',

    create_time DATETIME    not null COMMENT '创建时间',
    PRIMARY KEY (id)
);
create table t_item_003
(

    id          bigint auto_increment COMMENT '自动增长id',

    item_name   varchar(10) not null default '' COMMENT 'item名称',

    item_count  int         not null default 0 COMMENT 'item数量',

    create_time DATETIME    not null COMMENT '创建时间',
    PRIMARY KEY (id)
);